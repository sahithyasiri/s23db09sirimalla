//- extends layout

//- block content
//-   h1= title
//-   p sahithya sirimalla
//-   p Welcome to #{title}
//-   p This is my repo
//-   button(type="button" class="btn btn-secondary" onclick="doReduce()") Press Me
//-   a(href="http://localhost:3000/icecream") Click Here to Visit icecream Website
//-   div#reduce Reduce the value
//-   script.
//-     let count = 20;
//-     let change = -1;
//-     function doReduce() {
//-       count += change;
//-       change -= 1;
//-       document.getElementById('reduce').textContent = `Count ${count} with change ${change}`;
//-     }
//-   //a(href="http://localhost:3000/cars") Click Here to Visit Cars Website
//-   //a(href="cars") cars page
  
extends layout

block content
  if (!user)
    a(href="/login") Login
    br
    a(href="/register") Register
  if (user)
    p You are currently logged in as #{user.username}
    a(href="/icecream") Take me to icecream
    br
    a(href="/logout") Logout
