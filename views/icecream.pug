extends layout

block content
  h1= title
  - var ice= results
  div(class="container-fluid")
  div(class="row")
   each i,index  in ice
    div(class="col-sm-2" onclick =`buy(${index})`) #{i.icecream_flavour}, #{i.icecream_scoops}, #{i.icecream_cost}
    script.
        function buy(a)
        {
            alert(`You purchased Item ${a+1}`)
        }
